import { logs, devLog } from "../../../utils/index.js";

// 🟢 Initialer Aufruf
function initModul() {
    devLog (`Funktion für Page "Php Information im Testmodus aussgeben"`, `info`);
}

export { initModul };